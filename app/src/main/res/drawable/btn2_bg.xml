<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background Color -->
    <solid android:color="#088E0E"/>

    <!-- Border Color -->
    <stroke
        android:width="1dp"
        android:color="#088E0E"/>

    <!-- Rounded corners -->
    <corners android:radius="8dp"/>

    <!-- Padding inside EditText (optional) -->
    <padding
        android:left="10dp"
        android:top="8dp"
        android:right="10dp"
        android:bottom="8dp"/>
</shape>
